# TODO: Complete this program
name = input("Enter student name: ")
score = float(input("Enter score (0-100): "))
attendance = float(input("Enter attendance %: "))
# Use conditional expressions to set:
# 1. Grade letter (A/B/C/D/F)
# 2. Pass/Fail status
# 3. Honor roll (Yes if >= 90 and attendance >= 95)
# 4. Recommendation (different messages based on performance)
# Create a complete report using these values
#Your Code here:
